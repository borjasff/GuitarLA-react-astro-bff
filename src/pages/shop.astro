---
import Layout from '../layouts/Layout.astro';
import Guitarra from '../components/Guitarra.astro';

const url = `${import.meta.env.PUBLIC_API_URL}/guitarras?populate=image`
const answer = await fetch(url)
const {data} = await answer.json()
console.log(data)
---

<Layout title="Shop" description="Guitar Shop, courses, blogs and more">
	<main class="conteiner">
		<h1 class="heading">Our Collection</h1>

		<div class="grid">
			{data?.map(guitarra => (
				<Guitarra
					name={guitarra.attributes.name}
					description={guitarra.attributes.description}
					url={guitarra.attributes.url}
					price={guitarra.attributes.price}
					image={guitarra.attributes.image.data.attributes.formats.medium.url}
				/>
			))}
		</div>

	</main>
</Layout>

<style>
	
</style>
